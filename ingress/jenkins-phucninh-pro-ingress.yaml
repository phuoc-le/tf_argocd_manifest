apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jenkins-phucninh-ingress
  namespace: infra
  annotations:
    cert-manager.io/cluster-issuer: selfsigned-cluster-issuer
    cert-manager.io/duration: 999999h
    cert-manager.io/renew-before: 999h
    cert-manager.io/common-name: 'jenkins.phucninh.pro'
    cert-manager.io/subject-organizations: 'PhucNinh .Inc'
    cert-manager.io/subject-organizationalunits: 'Technology Services'
    cert-manager.io/subject-countries: 'VN'
    cert-manager.io/subject-localities: 'HCM City'
    cert-manager.io/subject-postalcodes: '70000'
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
spec:
  tls:
    - hosts:
        - jenkins.phucninh.pro
      secretName: jenkins-phucninh-pro-tls
  ingressClassName: nginx
  rules:
    - host: jenkins.phucninh.pro
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jenkins
                port:
                  number: 8080
